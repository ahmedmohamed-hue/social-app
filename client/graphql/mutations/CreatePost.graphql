mutation createPost($title: String!, $body: String!) {
  createPost(options: { title: $title, body: $body }) {
    id
    body
    title
    createdAt
    creatorId
    owner
    likes
    likeStatus
    creator {
      username
      firstName
      lastName
    }
  }
}
