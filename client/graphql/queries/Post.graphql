query Post($id: Float!) {
  post(id: $id) {
    id
    body
    title
    createdAt
    creatorId
    owner
    likes {
      ...RegularUser
    }
    likeStatus
    creator {
      username
      firstName
      lastName
      avatar_url
    }
  }
}
