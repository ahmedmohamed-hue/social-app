query User($username: String!) {
  getUser(username: $username) {
    id
    email
    firstName
    username
    lastName
    onlineStatus
    lastSeen
    avatar_url
    posts {
      body
      title
      createdAt
      owner
      id
      likes
      likeStatus
    }
  }
}
