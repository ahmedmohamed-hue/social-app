query GetUser($username: String!) {
  getUser(username: $username) {
    id
    email
    firstName
    lastName
    onlineStatus
    lastSeen
    avatar_url
    posts {
      body
      title
      createdAt
      owner
      id
    }
  }
}
